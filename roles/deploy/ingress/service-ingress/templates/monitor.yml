---

apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: ingressroute-grafana
  namespace: default
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`monitor.mccannclan.family`)
      kind: Rule
      services:
        - name: grafana
          namespace: monitor
          port: 3000
  #  middlewares:
  #  - chain-authelia
  #  - chain-basic-auth
  #  - chain-no-auth
  tls:
    store:
      name: default
